[package]
name = "hypp-test"
version = "0.1.0"
authors = ["Audun Halland <audun.halldand@pm.me>"]
edition = "2018"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
hypp = { path = "../../" }
web-sys = { version = "0.3", features = ["console"] }
wasm-bindgen = "0.2"
